function pageHead(title,desc){return `<div class="page-head"><div><h2 class="page-title">${title}</h2><p class="page-desc">${desc}</p></div><span class="demo">Personal tracker</span></div>`}

const STATUS_ORDER=['Prepared','Applied','Under review','Interview completed','No outcome found','Talent pool / EOI','CV acknowledged','Rejected','Role withdrawn','Recruitment cancelled','Ad closed'];
const STATUS_COLORS={'Prepared':'var(--prepared)','Applied':'var(--accent)','Under review':'#2f7f8b','Interview completed':'var(--good)','No outcome found':'var(--warn)','Talent pool / EOI':'#5d9367','CV acknowledged':'#77a17e','Rejected':'var(--risk)','Role withdrawn':'var(--ink-3)','Recruitment cancelled':'#89959d','Ad closed':'#a4adb3'};
function statusEntries(){const map={};state.rows.forEach(r=>{if(r.Status)map[r.Status]=(map[r.Status]||0)+1});return STATUS_ORDER.filter(s=>map[s]).map(s=>[s,map[s]])}
function conicGradient(entries,total,colors=STATUS_COLORS){let pos=0;const stops=[];entries.forEach(([k,v],i)=>{const start=pos,end=pos+(v/total*100),color=colors[k]||['var(--accent)','var(--prepared)','var(--good)','var(--warn)','var(--risk)','var(--ink-3)'][i%6];stops.push(`${color} ${start.toFixed(3)}% ${end.toFixed(3)}%`);pos=end});return `conic-gradient(${stops.join(',')})`}
function chartLegend(entries,total,colors=STATUS_COLORS,showPct=true){return `<div class="legend" style="margin-top:0;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px 12px">${entries.map(([k,v],i)=>{const pct=total?Math.round(v/total*100):0,color=colors[k]||['var(--accent)','var(--prepared)','var(--good)','var(--warn)','var(--risk)','var(--ink-3)'][i%6];return `<span title="${esc(k)}"><i style="background:${color};border-radius:50%"></i><span style="min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${esc(shortStatus(k))}</span><strong style="margin-left:auto;color:var(--ink);font:600 .7rem var(--mono)">${showPct?pct+'%':v}</strong></span>`}).join('')}</div>`}
function statusBreakdown(){const entries=statusEntries(),total=entries.reduce((n,x)=>n+x[1],0)||1,gradient=conicGradient(entries,total);return `<div style="display:grid;grid-template-columns:minmax(150px,210px) 1fr;gap:22px;align-items:center;min-height:220px"><div style="display:grid;place-items:center"><div title="Status distribution" style="width:180px;aspect-ratio:1;border-radius:50%;background:${gradient};display:grid;place-items:center;box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--border) 55%,transparent)"><div style="width:100px;aspect-ratio:1;border-radius:50%;background:var(--surface);display:grid;place-items:center;text-align:center;box-shadow:0 0 0 1px var(--border)"><div><strong style="display:block;font:600 1.65rem/1 var(--serif)">${total}</strong><span style="display:block;margin-top:5px;font:400 .62rem var(--mono);text-transform:uppercase;letter-spacing:.06em;color:var(--ink-3)">records</span></div></div></div></div>${chartLegend(entries,total,STATUS_COLORS,true)}</div>`}
function treemap(entries){if(!entries.length)return `<div class="empty">No organisations to chart.</div>`;const total=entries.reduce((n,x)=>n+x[1],0),rects=[];function split(items,x,y,w,h){if(!items.length)return;if(items.length===1){rects.push({item:items[0],x,y,w,h});return}const sum=items.reduce((n,i)=>n+i[1],0),target=sum/2;let acc=0,cut=1,best=Infinity;for(let i=1;i<items.length;i++){acc+=items[i-1][1];const diff=Math.abs(target-acc);if(diff<best){best=diff;cut=i}}const a=items.slice(0,cut),b=items.slice(cut),sa=a.reduce((n,i)=>n+i[1],0),ratio=sa/sum;if(w>=h){const wa=w*ratio;split(a,x,y,wa,h);split(b,x+wa,y,w-wa,h)}else{const ha=h*ratio;split(a,x,y,w,ha);split(b,x,y+ha,w,h-ha)}}split(entries,0,0,100,100);const palette=['var(--accent)','var(--prepared)','var(--good)','var(--warn)','var(--risk)','var(--ink-3)'];return `<div style="position:relative;height:260px;border:1px solid var(--border);border-radius:8px;overflow:hidden;background:var(--surface-deep)">${rects.map((r,i)=>{const [name,value]=r.item,pct=Math.round(value/total*100),color=palette[i%palette.length],small=r.w<18||r.h<20;return `<div title="${esc(name)}: ${value} application${value===1?'':'s'} (${pct}%)" style="position:absolute;left:${r.x}%;top:${r.y}%;width:${r.w}%;height:${r.h}%;padding:${small?'6':'9'}px;border:2px solid var(--surface);background:color-mix(in srgb,${color} ${18+(i%3)*7}%,var(--surface));overflow:hidden"><strong style="display:block;font-size:${small?'.68rem':'.78rem'};line-height:1.25;white-space:${small?'nowrap':'normal'};overflow:hidden;text-overflow:ellipsis">${esc(name)}</strong><span style="display:block;margin-top:4px;font:400 .66rem var(--mono);color:var(--ink-2)">${value} · ${pct}%</span></div>`}).join('')}</div>`}

function weeklySeries(){const m=new Map();state.rows.forEach(r=>{const d=dateFrom(r['Applied date']);if(!d)return;const x=new Date(d),day=(x.getDay()+6)%7;x.setDate(x.getDate()-day);x.setHours(0,0,0,0);const k=x.toISOString().slice(0,10);m.set(k,(m.get(k)||0)+1)});return [...m.entries()].sort((a,b)=>a[0].localeCompare(b[0])).slice(-12)}
function lineChart(series){if(!series.length)return `<div class="empty">No dated applications yet.</div>`;const W=650,H=220,pL=32,pR=12,pT=15,pB=30,max=Math.max(1,...series.map(x=>x[1])),pts=series.map(([x,v],i)=>{const px=pL+i*(W-pL-pR)/(Math.max(1,series.length-1));const py=H-pB-(v/max)*(H-pT-pB);return [px,py];}),path=pts.map((p,i)=>`${i?'L':'M'}${p[0].toFixed(1)},${p[1].toFixed(1)}`).join(' '),area=`${path} L ${pts[pts.length-1][0].toFixed(1)} ${H-pB} L ${pts[0][0].toFixed(1)} ${H-pB} Z`;return `<svg class="svg-chart" viewBox="0 0 ${W} ${H}" preserveAspectRatio="none">${[0,.25,.5,.75,1].map(t=>{const y=pT+t*(H-pT-pB);return `<line class="gridline" x1="${pL}" y1="${y}" x2="${W-pR}" y2="${y}"/>`}).join('')}<path class="chart-fill" d="${area}"/><path class="chart-line" d="${path}"/>${pts.map((p,i)=>`<circle cx="${p[0]}" cy="${p[1]}" r="3.5" fill="var(--surface)" stroke="var(--accent)" stroke-width="2"><title>${series[i][0]}: ${series[i][1]}</title></circle>`).join('')}${series.map(([x,v],i)=>`<text class="axis" x="${pts[i][0]}" y="${H-8}" text-anchor="middle">${x.slice(5)}</text>`).join('')}</svg>`}

function deadlineRows(limit=8){return state.rows.filter(r=>r.Status==='Prepared').sort(preparedDeadlineSort).slice(0,limit)}
function deadlineMarkup(r){const n=daysUntil(r.Deadline),missing=n===null,label=missing?'—':n<0?`${Math.abs(n)}d late`:n===0?'Today':`${n}d`;return `<div class="deadline-row"><div class="days">${label}<span>${missing?'No date':'remaining'}</span></div><div class="deadline-main"><strong>${esc(r.Company)}</strong><p>${esc(r.Role)}${statusBadge(r.Status)}${r['Job ID']?`<span class="pill">${esc(r['Job ID'])}</span>`:''}</p></div><div class="deadline-date">${r.Deadline?esc(fmtDate(r.Deadline)):'No deadline'}</div>${linkFor(r,'Apply link')?`<a class="mini-link" href="${esc(linkFor(r,'Apply link'))}" target="_blank" rel="noopener">Apply</a>`:`<span class="badge b-neutral">No link</span>`}</div>`}
function attentionItems(){const out=[];state.rows.forEach(r=>{if(r.Status==='Prepared'){const n=daysUntil(r.Deadline);if(n===null)out.push(['Missing deadline',r]);else if(n<0)out.push(['Prepared but expired',r]);else if(n<=3)out.push(['Deadline imminent',r]);if(!linkFor(r,'Apply link'))out.push(['Missing apply link',r])}if(r.Status==='No outcome found'){const d=dateFrom(r['Applied date']);if(d&&Date.now()-d.getTime()>30*86400000)out.push(['No outcome >30 days',r])}});return out.slice(0,8)}

function overview(){const c=counts(),dated=state.rows.filter(r=>r['Applied date']).length,awaiting=state.rows.filter(r=>['Applied','Under review','No outcome found'].includes(r.Status)).length,att=attentionItems();return `${pageHead('Overview','The application pipeline, immediate deadlines and the records that need attention.')}<section class="grid kpis"><div class="card"><div class="eyebrow">Tracked</div><div class="kpi-value">${c.total}</div><div class="kpi-sub">All application records</div></div><div class="card"><div class="eyebrow">Prepared</div><div class="kpi-value">${c.prepared}</div><div class="kpi-sub">Ready to submit</div></div><div class="card"><div class="eyebrow">Awaiting</div><div class="kpi-value">${awaiting}</div><div class="kpi-sub">Submitted without a close outcome</div></div><div class="card"><div class="eyebrow">Dated submissions</div><div class="kpi-value">${dated}</div><div class="kpi-sub">Rows with an applied date</div></div></section><section class="grid two"><div class="card"><div class="section-title"><div><h3>Application activity</h3><p>Submissions by week.</p></div></div>${lineChart(weeklySeries())}</div><div class="card"><div class="section-title"><div><h3>Status distribution</h3><p>Current status mix by percentage.</p></div></div>${statusBreakdown()}</div></section><section class="card" style="margin-top:18px"><div class="section-title"><div><h3>Attention</h3><p>Deadline, aging and link checks.</p></div></div>${att.length?att.map(([t,r])=>`<div class="attention-row" data-open="${r._row}"><strong>${esc(t)}</strong><div>${esc(r.Company)} · ${esc(r.Role)}</div><span class="row-chevron">›</span></div>`).join(''):`<div class="notice">Nothing needs immediate attention.</div>`}</section><section class="card" style="margin-top:18px"><div class="section-title"><div><h3>Next prepared applications</h3><p>Missing deadlines first, then earliest closing dates.</p></div><button class="btn soft" data-goto="deadlines">View all</button></div><div class="deadline-list">${deadlineRows().map(deadlineMarkup).join('')||`<div class="empty">No prepared applications in this workbook.</div>`}</div></section>`}

function laneRows(type){let rows=[];if(type==='prepared')rows=state.rows.filter(r=>r.Status==='Prepared').sort(preparedDeadlineSort);if(type==='submitted')rows=state.rows.filter(r=>['Applied','Under review','No outcome found','CV acknowledged','Talent pool / EOI'].includes(r.Status));if(type==='interview')rows=state.rows.filter(r=>r.Status==='Interview completed');if(type==='closed')rows=state.rows.filter(r=>CLOSED.has(r.Status));return rows}
function cardMarkup(r){const n=daysUntil(r.Deadline),meta=r.Status==='Prepared'?(r.Deadline?(n===null?'':n<0?'Expired':n===0?'Due today':`${n}d`):'No deadline'):(r['Applied date']?`Applied ${fmtDate(r['Applied date'])}`:'');return `<div class="job-card" data-open="${r._row}"><div class="co">${esc(r.Company)}</div><div class="rl">${esc(r.Role)}</div><div class="meta"><span>${statusBadge(r.Status)}</span><span>${esc(meta)}</span></div></div>`}
function pipeline(){const lanes=[['prepared','Prepared'],['submitted','Submitted / awaiting'],['interview','Interview'],['closed','Closed']];return `${pageHead('Pipeline','Scan the current workflow by stage. Click a card for details and links.')}<div class="toolbar"><input class="search" id="pipeSearch" placeholder="Filter pipeline" value="${esc(state.filter)}"><label class="check"><input type="checkbox" id="pipeHide" ${state.hideClosed?'checked':''}> Hide closed</label></div><div class="kanban">${lanes.filter(([k])=>!(state.hideClosed&&k==='closed')).map(([k,t])=>{const rows=laneRows(k),q=keyNorm(state.filter),filtered=!q?rows:rows.filter(r=>keyNorm([r.Company,r.Role,r.Status].join(' ')).includes(q));return `<div class="lane"><div class="lane-head"><strong>${t}</strong><span>${filtered.length}</span></div>${filtered.length?filtered.map(cardMarkup).join(''):`<div class="empty">No ${t.toLowerCase()} records.</div>`}</div>`}).join('')}</div>`}

function toolbar(){const statuses=['All',...new Set(state.rows.map(r=>r.Status).filter(Boolean))];return `<div class="toolbar"><input class="search" id="appSearch" placeholder="Search company, role, ID, contact or notes" value="${esc(state.filter)}"><select class="select" id="statusFilter">${statuses.map(s=>`<option value="${esc(s)}" ${state.status===s?'selected':''}>${esc(s)}</option>`).join('')}</select><select class="select" id="companyFilter"><option>All</option>${companies().map(c=>`<option ${state.company===c?'selected':''}>${esc(c)}</option>`).join('')}</select><select class="select" id="sortFilter"><option value="deadline" ${state.sort==='deadline'?'selected':''}>Deadline</option><option value="recent" ${state.sort==='recent'?'selected':''}>Recent activity</option><option value="company" ${state.sort==='company'?'selected':''}>Company</option></select><label class="check"><input type="checkbox" id="hideClosed" ${state.hideClosed?'checked':''}> Hide closed</label><button class="btn" id="csvBtn">Export CSV</button></div>`}
function appTable(){const rows=filteredRows();return `${toolbar()}<div class="table-wrap"><table><thead><tr><th>Status</th><th>Company</th><th>Role</th><th>Applied</th><th>Deadline</th><th>Job ID</th><th>Contact</th><th>Links</th><th></th></tr></thead><tbody>${rows.map(r=>`<tr data-open="${r._row}"><td>${statusBadge(r.Status)}</td><td class="company">${esc(r.Company)}</td><td class="role">${esc(r.Role)}</td><td>${esc(fmtDate(r['Applied date']))}</td><td>${esc(fmtDate(r.Deadline))}</td><td>${esc(r['Job ID'])}</td><td>${esc(r.Contact||r['Contact email'])}</td><td><div class="link-row">${usefulLinks(r).slice(0,5).map(([l,u])=>u?`<a class="mini-link" href="${esc(u)}" target="_blank" rel="noopener">${esc(l)}</a>`:'').join('')}</div></td><td class="row-chevron">›</td></tr>`).join('')||`<tr><td colspan="9"><div class="empty">No applications match these filters.</div></td></tr>`}</tbody></table></div>${rows.length?`<div class="footer-note">Showing ${rows.length} of ${state.rows.length} records. Deadline sorting is the default.</div>`:''}`}
function applications(){return `${pageHead('Applications','Search and filter the register. Prepared roles without a deadline come first, then records are ordered by deadline.')}${appTable()}`}

function deadlines(){const prep=state.rows.filter(r=>r.Status==='Prepared').sort(preparedDeadlineSort),dated=prep.filter(r=>dateFrom(r.Deadline)),missing=prep.filter(r=>!dateFrom(r.Deadline));return `${pageHead('Deadlines','Prepared applications with missing deadlines first, followed by the earliest closing dates.')}<section class="grid kpis" style="grid-template-columns:repeat(3,1fr)"><div class="card"><div class="eyebrow">Prepared</div><div class="kpi-value">${prep.length}</div><div class="kpi-sub">Ready to submit</div></div><div class="card"><div class="eyebrow">Due within 7 days</div><div class="kpi-value">${dated.filter(r=>{const n=daysUntil(r.Deadline);return n!==null&&n>=0&&n<=7}).length}</div><div class="kpi-sub">Prioritise first</div></div><div class="card"><div class="eyebrow">Deadline missing</div><div class="kpi-value">${missing.length}</div><div class="kpi-sub">Shown at the top</div></div></section><div class="card"><div class="section-title"><div><h3>Submission queue</h3><p>Countdowns use your local date.</p></div></div><div class="deadline-list">${prep.map(deadlineMarkup).join('')||`<div class="empty">No prepared applications in this workbook.</div>`}</div></div>`}

function quality(){const missingApplied=state.rows.filter(r=>r.Status!=='Prepared'&&!r['Applied date']),missingDeadline=state.rows.filter(r=>r.Status==='Prepared'&&!dateFrom(r.Deadline)),missingApply=state.rows.filter(r=>r.Status==='Prepared'&&!linkFor(r,'Apply link')),missingFolder=state.rows.filter(r=>!linkFor(r,'Application folder')),missingCv=state.rows.filter(r=>r.Status==='Prepared'&&!linkFor(r,'Tailored CV')),missingCover=state.rows.filter(r=>r.Status==='Prepared'&&!linkFor(r,'Cover letter'));const issues=[['Missing applied date',missingApplied],['Prepared without deadline',missingDeadline],['Prepared without apply link',missingApply],['Missing application folder',missingFolder],['Prepared without CV link',missingCv],['Prepared without cover letter link',missingCover]],totalIssues=issues.reduce((n,x)=>n+x[1].length,0);return `${pageHead('Data quality','Checks the workbook you loaded. Nothing is fetched from GitHub or Google Sheets.')}<section class="grid kpis" style="grid-template-columns:repeat(3,1fr)"><div class="card"><div class="eyebrow">Records</div><div class="kpi-value">${state.rows.length}</div><div class="kpi-sub">Loaded from ${esc(state.source||'No workbook')}</div></div><div class="card"><div class="eyebrow">Potential issues</div><div class="kpi-value">${totalIssues}</div><div class="kpi-sub">Across six practical checks</div></div><div class="card"><div class="eyebrow">Worksheet</div><div class="kpi-value" style="font-size:1.25rem">${esc(state.sheet)}</div><div class="kpi-sub">Workbook table source</div></div></section><div class="card"><div class="section-title"><div><h3>Checks</h3><p>These are completeness flags, not automatic corrections.</p></div><button class="btn soft" id="qualityReload">Replace workbook</button></div><div class="data-grid">${issues.map(([label,rows])=>`<div class="data-item"><div class="v">${rows.length}</div><div class="l">${esc(label)}</div></div>`).join('')}</div>${totalIssues?`<div class="section-title" style="margin-top:18px"><div><h3>Examples</h3><p>First rows in each issue set.</p></div></div><div class="attention-list">${issues.filter(([,rows])=>rows.length).map(([label,rows])=>`<div class="section-title"><div><h3>${esc(label)}</h3><p>${rows.slice(0,8).map(r=>`${esc(r.Company||'Unknown')} · ${esc(r.Role||'Role')}`).join(' | ')}</p></div></div>`).join('')}</div>`:''}</div>`}

function attachViewEvents(){document.querySelectorAll('[data-open]').forEach(el=>el.addEventListener('click',()=>openDrawer(+el.dataset.open)));document.querySelectorAll('[data-goto]').forEach(el=>el.addEventListener('click',()=>{state.view=el.dataset.goto;localStorage.setItem('jobDashView',state.view);render()}));const ps=document.getElementById('pipeSearch');if(ps)ps.oninput=e=>{state.filter=e.target.value;render()};const ph=document.getElementById('pipeHide');if(ph)ph.onchange=e=>{state.hideClosed=e.target.checked;render()};const s=document.getElementById('appSearch');if(s)s.oninput=e=>{state.filter=e.target.value;render()};const sf=document.getElementById('statusFilter');if(sf)sf.onchange=e=>{state.status=e.target.value;render()};const cf=document.getElementById('companyFilter');if(cf)cf.onchange=e=>{state.company=e.target.value;render()};const so=document.getElementById('sortFilter');if(so)so.onchange=e=>{state.sort=e.target.value;render()};const hc=document.getElementById('hideClosed');if(hc)hc.onchange=e=>{state.hideClosed=e.target.checked;render()};const csv=document.getElementById('csvBtn');if(csv)csv.onclick=exportCsv;const qr=document.getElementById('qualityReload');if(qr)qr.onclick=()=>document.getElementById('fileInput').click()}

function render(){if(!state.rows.length)return;renderSidebar();const views={overview,pipeline,applications,deadlines,quality};document.getElementById('main').innerHTML=(views[state.view]||overview)();attachViewEvents()}